# Run and deploy NetWitness Visualizer

This contains everything you need to run your app locally.

## Run Locally

1. Install dependencies:
   `sudo apt install nodejs npm`
2. Move all files to your folder
3. Run the app:
   `npm run dev`
4. Access the app by browsing to: http://ip_address:3000
