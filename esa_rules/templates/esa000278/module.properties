# Module Definition Properties
module_name=[NWC] Possible SQL Injection
module_desc=Detects when SQL injection signatures are seen targeting internal devices (inbound or lateral) X number of times between the same source IP and the same destination IP within Y seconds. X and Y are configurable.
module_desc_long=This rule detects when SQL injection signatures are seen targeting internal devices (inbound or lateral) X number of times between the same source IP and the same destination IP within Y seconds. X and Y are configurable.\r\n\r\nThe IP Whitelist field allows to add a comma separated list of known and trusted IP addresses that are known and expected to perform this behavior. Use an empty space to clear the field. Multiple values must be comma separated.

# FTL Template Variables
time_window_name=Within this number of seconds
time_window_desc=Within this number of seconds

count_name=Triggers for that many connection attempts
count_desc=Triggers for that many connection attempts

ip_srclist_name=List of source IP addresses to whitelist
ip_srclist_desc=List of source IP addresses to whitelist

ip_dstlist_name=List of destination IP addresses to whitelist
ip_dstlist_desc=List of destination IP addresses to whitelist